# Codex 项目规范：GoCapture 本地工具边界

## 开发前强制阅读

Codex 在本项目中进行任何代码修改前，必须先阅读本文件。

尤其是修改以下模块前，必须重新核对本规范：

1. DOM Agent
2. 本地源码检索
3. 候选淘汰
4. 源码关系图
5. 经验系统
6. Prompt Enhancement
7. Agent Host / Tools / MCP / Experience 相关代码

如果实现方案和本文件冲突，必须停止开发，先和用户确认。

## 最高原则

在 GoCapture 项目中，Codex 不得让本地工具承担框架理解职责。

本地工具只负责收集事实、执行检索、裁剪证据、过滤明显无效命中、减少 LLM 输入压力。

所有涉及框架语义、渲染归属、组件关系、数据流含义、业务意图、源码责任归属的判断，必须交给 LLM 基于真实证据完成。

## 禁止事项

Codex 修改 GoCapture 时，禁止写入以下类型的本地规则：

1. 禁止用固定正则判断某种框架语法是否代表渲染文件。
2. 禁止写死 Vue、React、Naive UI、Element、iView、JSX、template、setup、render、h、createElement 等框架规则。
3. 禁止根据文件后缀直接判断 `.vue`、`.tsx`、`.jsx`、`.js`、`.ts` 是否是最终渲染 owner。
4. 禁止根据目录名、文件名、变量名直接推断业务角色。
5. 禁止把 import/export/router/store/hook/component 等语法关系直接解释为业务事实。
6. 禁止因为本地分数高、路径近、命中文案多，就直接认定最终源码位置。
7. 禁止为了修复单个项目案例写场景补丁。
8. 禁止把用户提供的具体 DOM、文案、路径、变量名、class 名写成硬编码规则。

## 本地工具允许做的事

本地工具只能做确定性工作：

1. 按 LLM 给出的计划检索源码。
2. 读取文件、目录、片段、引用路径。
3. 提取 DOM、源码、路径、文件结构中的原始事实。
4. 淘汰明显无效命中：
   - 注释命中
   - console/log 输出命中
   - class 误命中对象属性
   - 文案非完整匹配
   - 原生 DOM 标签结构明显不一致
5. 记录候选文件、命中行、代码片段、路径引用、文件邻近关系。
6. 压缩证据，减少 LLM 需要阅读的内容。
7. 证据不足时返回需要扩区或继续检索，而不是猜测。

## LLM 必须负责的事

以下判断只能由 LLM 基于本地证据完成：

1. 当前 DOM 是否由某候选文件生成。
2. 候选文件是页面、组件、配置、路由、数据源、国际化、公共组件还是子组件。
3. 多个候选文件之间是否能组成完整渲染链路。
4. 定义文件是否流向渲染文件。
5. 某个候选是否只是相似文案或配置命中。
6. 当前证据是否足够定位源码。
7. 用户需求最终应该落在哪个文件和代码区域。

## 正确工作流

```text
LLM 判断下一步要验证什么
        ↓
本地工具执行检索 / 读取 / 裁剪 / 淘汰
        ↓
本地工具返回真实证据
        ↓
LLM 判断关系是否成立
        ↓
证据不足则继续调用工具或申请扩区
```

本地工具不能越权成为语义裁决器。

## 代码修改自检

Codex 每次修改 DOM Agent、经验系统、源码定位、检索器、关系图、候选裁决相关代码时，都必须先检查：

1. 是否引入了框架专属判断？
2. 是否把某个案例写成了固定规则？
3. 是否让本地工具替代 LLM 做了语义判断？
4. 是否还能适用于未知框架、未知语法、未知项目结构？
5. 是否只是减少 LLM 压力，而不是剥夺 LLM 推理职责？

如果答案不明确，必须停止实现，先和用户确认方案。
